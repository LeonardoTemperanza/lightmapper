#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct main0_out
{
    float4 out_var_SV_TARGET [[color(0)]];
};

fragment main0_out main0(texture2d<float> tex [[texture(0)]], sampler s [[sampler(0)]], float4 gl_FragCoord [[position]])
{
    main0_out out = {};
    out.out_var_SV_TARGET = tex.sample(s, float2((gl_FragCoord.x * 0.5) + 0.5, 0.5 - (gl_FragCoord.y * 0.5)));
    return out;
}

